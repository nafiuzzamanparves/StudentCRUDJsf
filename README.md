```
CREATE TABLE studentJsf (
    id NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100) NOT NULL,
    email VARCHAR2(100) NOT NULL UNIQUE,
    course VARCHAR2(100) NOT NULL
);
```
